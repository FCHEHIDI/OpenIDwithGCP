# Workflow Diagram - OpenID Connect avec JWT

## Vue d'ensemble du flux

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        1. PAGE D'ACCUEIL (/)                            │
│  Code: templates.TemplateResponse("home.html", {"user": user})          │
└────────────────────────┬────────────────────────────────────────────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │ Cookie access_token? │
              └──────────┬───────────┘
                    ┌────┴────┐
                 NON│         │OUI
                    │         │
         ┌──────────▼──┐   ┌──▼───────────────┐
         │ Non connecté│   │ Vérifier JWT     │
         │ Afficher btn│   │ verify_jwt_token │
         │ "Connexion" │   └──┬───────────────┘
         └──────┬──────┘      │
                │      ┌──────┴────────┐
                │   VALIDE        INVALIDE
                │      │               │
                │   ┌──▼────────────┐  │
                │   │ Profil user   │  │
                │   │ name, picture │  │
                │   └───────────────┘  │
                └──────────────┬───────┘
                               │
┌──────────────────────────────▼─────────────────────────────────────────┐
│               2. CLIC "SE CONNECTER AVEC GOOGLE"                       │
│  Code: await oauth.google.authorize_redirect(request, redirect_uri)    │
└────────────────────────────┬───────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│             3. REDIRECTION VERS GOOGLE OAUTH                            │
│  URL: https://accounts.google.com/o/oauth2/v2/auth                      │
│  Params: client_id, redirect_uri, scope, response_type=code             │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│       4. UTILISATEUR S'AUTHENTIFIE SUR GOOGLE                           │
│  - Entrer email/mot de passe                                            │
│  - Accepter permissions (openid, email, profile)                        │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│     5. GOOGLE REDIRIGE AVEC CODE D'AUTORISATION                         │
│  URL: http://localhost:8000/auth/callback?code=XXX&state=YYY            │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│           6. ÉCHANGER CODE CONTRE TOKEN (Serveur à Serveur)             │
│  Code: token = await oauth.google.authorize_access_token(request)       │
│  → POST vers https://oauth2.googleapis.com/token                        │
│  → Envoie: code, client_id, client_secret, redirect_uri                 │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│               7. RÉCUPÉRER INFORMATIONS UTILISATEUR                     │
│  Code: user_info = token.get('userinfo')                                │
│  Contenu: { email, name, picture, sub, email_verified }                 │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    8. CRÉER JWT                                         │
│  Code: user_data = {                                                    │
│    'email': user_info.get('email'),                                     │
│    'name': user_info.get('name'),                                       │
│    'picture': user_info.get('picture'),                                 │
│    'sub': user_info.get('sub')                                          │
│  }                                                                      │
│  expire = datetime.utcnow() + timedelta(minutes=60)                     │
│  user_data.update({"exp": expire, "iat": datetime.utcnow()})            │
│  jwt_token = jwt.encode(user_data, SECRET_KEY, algorithm="HS256")       │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                9. STOCKER JWT DANS COOKIE HTTPONLY                      │
│  Code: response.set_cookie(                                             │
│    key="access_token",                                                  │
│    value=jwt_token,                                                     │
│    httponly=True,      # Protection XSS                                 │
│    max_age=3600,       # 60 minutes                                     │
│    samesite="lax"      # Protection CSRF partielle                      │
│  )                                                                      │
│  return RedirectResponse(url='/')                                       │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│           10. UTILISATEUR CONNECTÉ - AFFICHER PROFIL                    │
│  Page affiche: photo, nom, email                                        │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
           ┌─────────────────┴─────────────────┐
           │                                   │
           ▼                                   ▼
┌─────────────────────────┐        ┌─────────────────────────┐
│ 11a. ACCÈS ROUTE        │        │ 11b. DÉCONNEXION        │
│     PROTÉGÉE            │        │    /auth/logout         │
│  /api/user              │        │                         │
│  /api/protected         │        │ Code:                   │
└──────────┬──────────────┘        │ response.delete_cookie  │
           │                       │  ("access_token")       │
           ▼                       └─────────────────────────┘
┌─────────────────────────────────────────────┐
│  12. EXTRAIRE ET VÉRIFIER JWT               │
│  Code: token = request.cookies.get(...)     │
│  user = jwt.decode(token, SECRET_KEY,       │
│                    algorithms=["HS256"])    │
│                                             │
│  Si valide: return user                     │
│  Si invalide: raise HTTPException(401)      │
└─────────────────────────────────────────────┘
```

## Légende des composants

### 🔵 Endpoints FastAPI
- **`@app.get("/")`** - Page d'accueil avec template Jinja2
- **`@app.get("/auth/login")`** - Initie le flux OAuth2
- **`@app.get("/auth/callback")`** - Callback OAuth2 de Google
- **`@app.get("/auth/logout")`** - Déconnexion et suppression JWT
- **`@app.get("/api/user")`** - Route protégée par JWT
- **`@app.get("/api/protected")`** - Autre route protégée

### 🔐 Fonctions de sécurité
```python
def create_jwt_token(user_data: dict) -> str:
    to_encode = user_data.copy()
    expire = datetime.utcnow() + timedelta(minutes=60)
    to_encode.update({"exp": expire, "iat": datetime.utcnow()})
    encoded_jwt = jwt.encode(to_encode, SECRET_KEY, algorithm="HS256")
    return encoded_jwt

def verify_jwt_token(token: str) -> dict:
    payload = jwt.decode(token, SECRET_KEY, algorithms=["HS256"])
    return payload

def get_current_user(request: Request) -> dict:
    token = request.cookies.get("access_token")
    if not token:
        raise HTTPException(status_code=401)
    return verify_jwt_token(token)
```

### 🌐 OAuth avec Authlib
```python
oauth = OAuth()
oauth.register(
    name='google',
    client_id=GOOGLE_CLIENT_ID,
    client_secret=GOOGLE_CLIENT_SECRET,
    server_metadata_url='https://accounts.google.com/.well-known/openid-configuration',
    client_kwargs={'scope': 'openid email profile'}
)
```

### 🎨 Templates Jinja2
```python
templates = Jinja2Templates(directory="templates")

@app.get("/")
async def home(request: Request):
    user = None
    token = request.cookies.get("access_token")
    if token:
        try:
            user = verify_jwt_token(token)
        except:
            pass
    return templates.TemplateResponse("home.html", {
        "request": request,
        "user": user
    })
```

## 🔑 Points clés du workflow

1. **Séparation des responsabilités** : OAuth Google (authentification) vs JWT (session)
2. **Sécurité multi-couches** : HttpOnly cookies + JWT signé + SameSite Lax
3. **Stateless** : Le JWT contient toutes les infos, pas de session serveur
4. **Expiration automatique** : JWT expire après 60 minutes (`exp` claim)
5. **Validation systématique** : Chaque requête protégée vérifie la signature JWT
